{
  "name": "sass-utils",
  "version": "1.0.0",
  "description": "Sweet sweet general sass mixins and css classes on an opt-in opt-out basis",
  "main": "index.js",
  "scripts": {
    "compile": "node-sass -o css --output-style compressed ./src/global",
    "copy": "cp -r src/abstract/ scss; cp css/all.css css/min.css",
    "build": "npm run compile; npm run copy",
    "release": "node ./scripts/release.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/brendanatme/sass-utils.git"
  },
  "author": "brendanatme",
  "license": "ISC",
  "homepage": "https://github.com/brendanatme/sass-utils",
  "devDependencies": {
    "node-sass": "^4.9.2",
    "normalize.css": "^8.0.0"
  }
}
